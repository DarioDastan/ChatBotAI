services:
  - type: web
    name: izocrete-chatbot
    env: python
    buildCommand: pip install -r requirements-light.txt
    startCommand: uvicorn chat_api_light:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 